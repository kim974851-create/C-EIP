#include <stdio.h>

int main() {
    // [1] 2차원 배열 선언 및 초기화
    //     메모리에 연속으로 배치됨:
    //     [ 10 | 20 | 30 | 40 | 50 | 60 ]
    //      a[0][0] a[0][1] a[0][2] a[1][0] a[1][1] a[1][2]
    int a[2][3] = { {10, 20, 30}, {40, 50, 60} };

    // [2] 2차원 배열 포인터 선언
    //     ptr은 "int 3개짜리 배열"을 가리키는 포인터
    //     a = &a[0] 이므로 ptr은 a[0] 행의 시작 주소를 담음
    //
    //     ptr → [ 10 | 20 | 30 ]  ← ptr[0] (= a[0], ptr+0)
    //            [ 40 | 50 | 60 ]  ← ptr[1] (= a[1], ptr+1)
    //
    //     ptr+1 이동 시: sizeof(int[3]) = 12바이트 점프
    int (*ptr)[3] = a;

    // [3] 첫 번째 printf
    //     a[0][1]   : 0행 → 시작주소, +1열 → 20
    //     ptr[0][1] : ptr+0 → a[0] 행, +1열 → 20
    //     *(*(ptr+0)+1) 과 동일
    //                    ptr
    //                     ↓
    //     [ 10 | >>20<< | 30 ]  ← ptr[0][1] = a[0][1] = 20
    //     [ 40 |  50   | 60 ]
    printf("%d, %d\n", a[0][1], ptr[0][1]);   // 출력: 20, 20

    // [4] 두 번째 printf
    //     a[1][2]   : 1행 → +12바이트, +2열 → 60
    //     ptr[1][2] : ptr+1 → a[1] 행, +2열 → 60
    //     *(*(ptr+1)+2) 과 동일
    //
    //     [ 10 |  20  |  30  ]
    //     [ 40 |  50  | >>60<< ]  ← ptr[1][2] = a[1][2] = 60
    //                    ↑
    //                 ptr+1 지점에서 +2
    printf("%d, %d\n", a[1][2], ptr[1][2])